import React from 'react';
/*
import DisplayImage from './DisplayImage';

// Assuming you have an array of menu items with image names
const menuItems = [
  { name: 'Nuggets', image: 'v1690734927/react_app_ourmenu/Nuggets_q0i8jf.jpg'},
  // Add more menu items with their respective image names
];

// add to Menu div variable
      {menuItems.map((item) => (
        <div key={item.name}>
          <h2>{item.name}</h2>
          <DisplayImage imageName={item.image} />
        </div>
      ))}

*/
const Menu = () => {
  return (
    <div>

    </div>
  );
};

export default Menu;
